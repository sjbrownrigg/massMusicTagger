____________________________________________________
  Name : ${album.artist}, ${album.title}
 Label : ${album.labels[0] if album.labels else ''}
 Genre : ${album.genre}
 Catno : ${album.catnumbers[0] if album.catnumbers else ''}
  Year : ${album.year}
   URL : ${album.url}

Master : http://www.discogs.com/master/${album.master_id}
____________________________________________________

% for disc in album.discs:
    % for track in disc.tracks:
  ${str(track.tracknumber).zfill(2)} ${(track.artist + ' - ') if track.artist and track.artist != album.artist else ''}${track.title}
    % endfor
% endfor
